本文是针对于v0.1.0疑问解答.md的逐条的进一步追问，请你了解两个文章的内容后，仔细思考后，拿出解决方案给我，等我确认并发令后再动代码具体修改

1.  条件导入（if块内）、try-except导入现在默认在requirements里是不导入的注释，我觉得应当默认是要导入的，方便小白一键上手使用这个工具，当然提醒肯定也是要做的。然后就是标签我觉得统一提示optional不太好，最好细化一下，requirements里给用户提示到底是条件导入还是try except导入，指出代码所在处我觉得很好。
2.  动态导入代码是怎么区分可静态解析还是不可静态解析的呢。我没搞明白这在代码上是可以实现的吗，为什么不可静态解析的出来一定是带着“<>”的呢，这有什么依据吗。
3.  Python版本你默认使用的是目前这个工具包开发的python版本还是检测目标项目虚拟环境或者python环境的python版本呢。虽然你有手动指定功能，但是如果使用的默认环境是开发这个工具时的python环境那不太合理吧，小白上手使用可能会报错。还有，不同python版本的标准库的数据你是怎么得到的呢
4.  错误标记部分，那如果说错误标记是指解析失败的时候，那是否应该在requirements里向用户报告错误的信息和对应代码的位置呢
5.  Backports数据是否齐全呢，难道这就是上面我问的不同python版本的标准库的数据，那么这个数据的齐全性完备性就很关键，因为你只记录了差异的部分
6.  “

\### 4.1 经典不匹配映射是否全面

\*\*当前 \`hardcoded.py\` 包含约 100+ 映射\*\* (\`hardcoded.py:10-171\`):

主要分类:

\- Computer Vision: \`cv2\`, \`cv\`

\- Image Processing: \`PIL\`, \`skimage\`

\- Machine Learning: \`sklearn\`, \`xgboost\`, \`lightgbm\`, \`catboost\`

\- Data Science: \`yaml\`, \`bs4\`, \`lxml\`

\- Cryptography: \`Crypto\`, \`Cryptodome\`, \`nacl\`, \`OpenSSL\`

\- Web Frameworks: \`flask\`, \`django\`, \`fastapi\`

\- Database: \`MySQLdb\`, \`pymysql\`, \`psycopg2\`, \`pymongo\`

\- 等等...

\*\*建议补充的映射\*\*:

\- \`dateparser\` -> \`dateparser\`

\- \`schedule\` -> \`schedule\`

\- \`watchdog\` -> \`watchdog\`

\- \`sh\` -> \`sh\`

\- \`arrow\` -> \`arrow\`

\- \`pendulum\` -> \`pendulum\`

\- \`keyring\` -> \`keyring\`

”既然你也说经典不匹配映射不完全全面，我建议你还是搜索资料尽量补充一些这种映射

1.  关于Pth注入模块，你觉得pywin32已经是最主要的是吗，那我觉得都行，加不加其他的都行，你看着来吧
2.  命名空间也是，既然你觉得不全，那还是搜集资料把它尽量补全吧。
3.  关于爬取wheel的toplevel数据时的错误日志，既然你也觉得“**\*\*当前局限\*\***:

1\. 只知道失败数量，不知道具体是哪些包失败

2\. 不知道失败原因（网络超时？404？解析错误？）

”那我觉得可以，最起码让开发者知道失败原因和查看实体日志

10、关于一对多的处理：“**\*\*requirements.txt 输出\*\***:

\`\`\`

opencv-python

\# === Packages with multiple candidates ===

\# cv2 -> Selected: opencv-python, Alternatives: opencv-contrib-python, opencv-python-headless

\`\`\`

”我觉得处理的不错，但是requirements里应该在注释里写其他包的所有选项的时候在后面加上下载量或者其他选择依据以提示用户，不过这是小问题